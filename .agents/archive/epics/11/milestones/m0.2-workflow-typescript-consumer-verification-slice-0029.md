# Milestone 0.2 Slice 0029: Workflow TypeScript Consumer Verification

Date: 2026-06-26

## Objective

Add first downstream consumer verification for the primary `WorkflowInstance` Oracle fixture by checking the manual TypeScript workflow contract shape against `workflow-instance.golden.json`.

## Capability

Canonical contract Oracle.

## Files Changed

- `src/CommandCenter.UI/src/test/characterization/workflowContractFixture.test.ts`
- `docs/contracts.md`
- `docs/architectural-mechanisms.md`
- `docs/architectural-capabilities.md`

## Evidence

- Added `workflowContractFixture.test.ts`, which reads `tests/CommandCenter.Backend.Tests/ContractFixtures/workflow-instance.golden.json` as backend Oracle truth.
- Added a typed schema map using `satisfies ObjectSchema<...>` for the manual TypeScript `WorkflowInstance` shape and represented nested workflow projection shapes.
- Added recursive runtime comparison that reports missing backend fixture fields, unexpected manual TypeScript fields, and object/array kind mismatches.
- The first fixture state preserves `decisionSession: null`; populated decision-session nested arrays are intentionally not exercised by this slice.

## Consumer Findings

- Manual TypeScript `WorkflowInstance` shape currently matches the `workflow-instance.golden.json` fixture for the represented fixture variant.
- Rust shell workflow commands for the primary workflow endpoint return `serde_json::Value` through the generic GET helper, so this slice reports no Rust workflow mirror drift for `WorkflowInstance`.
- `src/CommandCenter.UI/src/devTauriMock.ts` has no `get_workflow_projection` or broader `get_workflow_*` command handler coverage, so dev mock workflow response verification remains a coverage gap rather than a failure in this slice.

## Verification

- `npm run test -- --run src/test/characterization/workflowContractFixture.test.ts`
  - Result: 1 test file passed.
- `npm run build`
  - Result: passed.
- `npm run lint`
  - Result: passed.

## Limits

- No workflow artifact freshness manifest was added.
- No workflow request-boundary verifier was added for `GET /api/repositories/{repositoryId}/workflow`.
- No populated `decisionSession` fixture variant was added.
- No sibling workflow endpoint fixture or consumer verifier was added.
- No dev mock workflow handler was implemented or verified.
- No local workflow Oracle certification exists yet.
- Existing untracked `docs/audits/` content was left untouched.

## Rollback

Remove `src/CommandCenter.UI/src/test/characterization/workflowContractFixture.test.ts` and revert the documentation status updates. The backend workflow fixture and Slice 0028 backend serialization comparison remain valid and unchanged.
