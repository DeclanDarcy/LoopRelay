# M0.2 Dev Mock Consumer Verification Slice 0014

Date: 2026-06-26

## Objective

Add repository dashboard `devTauriMock.ts` verification against the Oracle fixture using the same consumer shape and recursive comparison model as the Rust and TypeScript consumers.

## Capability

Canonical contract Oracle.

## New Evidence

- `ContractConsumerVerificationTests` now includes consumer category metadata in verifier specs and drift records.
- Rust dashboard drift is categorized as a runtime consumer finding.
- TypeScript dashboard type verification is categorized as compile-time consumer verification.
- `devTauriMock` dashboard entry verification is categorized as development/test consumer verification.
- Added `DevTauriMockShapeProvider` for the repository dashboard pilot.
- The mock provider parses `src/CommandCenter.UI/src/devTauriMock.ts`, extracts the `dashboardEntry(workspace)` returned object literal, resolves `workspace.*` references through the existing TypeScript workspace shape provider, parses inline object literals, and treats `.length` count projections as numeric fields.
- `RepositoryDashboardDevTauriMockMatchesGoldenFixture` verifies `dashboardEntry(workspace)` has no missing, extra, or value-kind drift against `repository-dashboard.golden.json`.
- `RepositoryDashboardDevTauriMockRecursivelyVerifiesInlineContinuityShape` verifies inline continuity summary fields and workspace-derived reasoning and decision-session summaries participate in the shared recursive verifier pipeline.

## Architectural Impact

The dev mock remains a downstream development/test compatibility consumer. It is not promoted to contract authority. Backend JSON serialization observed through the Oracle fixture remains canonical contract truth.

The consumer verification mechanism now has three extractor classes feeding the same comparison model:

- Rust runtime mirror extractor.
- TypeScript compile-time type extractor.
- Dev mock development/test shape extractor.

## Current Findings

- Rust `RepositoryDashboardProjection` still omits `$[].decisionSessionSummary`.
- TypeScript `RepositoryDashboardProjection` currently matches the repository dashboard Oracle fixture shape.
- `devTauriMock` `dashboardEntry(workspace)` currently matches the repository dashboard Oracle fixture shape.

## Verification

Passed:

```powershell
dotnet test tests/CommandCenter.Backend.Tests/CommandCenter.Backend.Tests.csproj --filter FullyQualifiedName~ContractConsumerVerificationTests
```

Result:

- 7 tests passed.

## Known Limits

- Coverage remains limited to the repository dashboard pilot fixture.
- The dev mock provider handles the current `dashboardEntry(workspace)` object literal pattern; it is not a general TypeScript AST parser.
- Empty-array item shape still depends on fixture variants with populated arrays.
- Additional mock command payloads, generated artifact freshness, command argument verification, and semantic reinterpretation classification remain pending.

## Next Suggested Slice

Add generated artifact freshness or stale-artifact verification for the repository dashboard pilot, or first extract the shared verifier/provider scaffolding if reuse pressure becomes high enough to justify moving it out of the test file.
