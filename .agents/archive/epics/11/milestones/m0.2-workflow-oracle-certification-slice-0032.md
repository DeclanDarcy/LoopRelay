# M0.2 Workflow Oracle Certification Slice 0032

Date: 2026-06-26

## Objective

Consolidate and locally certify the primary workflow projection Contract Oracle pilot without expanding the mechanism set or adding workflow endpoint variants.

## Capability

Canonical contract Oracle.

## Certification Scope

Certified locally for the primary workflow projection pilot only:

- backend JSON serialization compared against `tests/CommandCenter.Backend.Tests/ContractFixtures/workflow-instance.golden.json`,
- manual TypeScript `WorkflowInstance` response-shape verification against the backend golden fixture,
- request-boundary verification for the primary `GET /api/repositories/{repositoryId}/workflow` path across backend route, Rust Tauri command, and TypeScript API wrapper,
- artifact freshness verification for `src/CommandCenter.UI/src/types/workflow.ts` against the workflow Oracle fixture,
- the full backend test project.

This certification does not certify Milestone 0.2 as complete.

## Exit-Criteria Review

| Milestone 0.2 criterion | Current result |
| --- | --- |
| Every contract has an identified authority and consumer set. | Not met globally. Repository dashboard, repository workspace, and primary workflow projection now have field-level or gated contract evidence. |
| Contract lifecycle and versioning rules exist. | Partially met. Procedural lifecycle and Oracle change workflow exist; mechanical versioning remains incomplete. |
| Golden fixture comparison can detect backend serialization drift. | Met for repository dashboard, repository workspace, and primary workflow projection pilots. |
| No ambiguity remains about where contract authority lives. | Met for the three certified pilots; not met globally until remaining contract families receive field-level inventory and fixture gating evidence. |

## Evidence

- `docs/contracts.md` defines workflow Oracle authority, fixture gating, request-boundary verification, artifact freshness, and accepted certification gaps.
- `docs/contract-endpoint-catalog.md` inventories workflow endpoint scope and producer/consumer context from the earlier workflow inventory slices.
- `docs/architectural-mechanisms.md` records workflow fixture comparison, TypeScript consumer verification, request-boundary verification, artifact freshness, and local certification status.
- `docs/architectural-capabilities.md` records workflow pilot certification while keeping Milestone 0.2 active.

## Verification

Passed:

```powershell
dotnet test tests/CommandCenter.Backend.Tests/CommandCenter.Backend.Tests.csproj --filter FullyQualifiedName~ContractOracleFixtureTests
dotnet test tests/CommandCenter.Backend.Tests/CommandCenter.Backend.Tests.csproj --filter FullyQualifiedName~ContractConsumerVerificationTests
dotnet test tests/CommandCenter.Backend.Tests/CommandCenter.Backend.Tests.csproj --filter FullyQualifiedName~ContractGeneratedArtifactFreshnessTests
dotnet test tests/CommandCenter.Backend.Tests/CommandCenter.Backend.Tests.csproj --filter FullyQualifiedName~ContractRequestBoundaryTests
npm run test -- src/test/characterization/workflowContractFixture.test.ts
dotnet test tests/CommandCenter.Backend.Tests/CommandCenter.Backend.Tests.csproj
```

Sequential focused backend Oracle mechanism results:

- fixture comparison: 6 passed, 0 failed, 0 skipped,
- backend consumer-verification family: 11 passed, 0 failed, 0 skipped,
- artifact freshness: 6 passed, 0 failed, 0 skipped,
- request-boundary verification: 9 passed, 0 failed, 0 skipped.

Targeted UI workflow TypeScript verifier result: 1 passed, 0 failed.

Full backend result: 802 passed, 0 failed, 0 skipped.

Initial parallel `dotnet test` execution produced build-output file locks between concurrent test processes. The same checks passed when rerun sequentially, consistent with the existing serialized .NET execution quarantine.

## Accepted Gaps

- No dev mock workflow handler is verified. This is accepted for the initial workflow pilot because the current development mock is not the workflow response authority and no workflow mock handler is required to prove Oracle mechanism validity.
- No populated `decisionSession` workflow fixture variant exists. This is accepted for the initial workflow pilot because the current `decisionSession: null` fixture pins a valid serialized contract state; populated coverage remains a planned variant, not a blocker.
- Sibling workflow endpoints are not certified. Certification is limited to the primary projection endpoint.
- Workflow TypeScript verification remains a UI characterization verifier over a manual Phase 0 contract artifact, not generated Milestone 1.2 infrastructure.

## Certification Result

The primary workflow projection Oracle pilot is locally certified as an executable pilot mechanism. Milestone 0.2 remains active and uncertified at milestone level.

The certification strengthens the Milestone 0.2 architectural claim from repository-only repeatability to repeatability across a richer semantic contract family with nested lifecycle, diagnostics, gates, transitions, and compatibility fields.

## Next Suggested Slice

Record a cross-family Oracle repeatability checkpoint covering repository dashboard, repository workspace, and primary workflow projection. The checkpoint should state whether Milestone 0.2 now needs another representative family, such as error envelope or decision lifecycle eligibility, before moving toward milestone-level certification.
