# Milestone 0.2 Slice 0030: Workflow Request Boundary

Date: 2026-06-26

## Objective

Add request-boundary verification for the primary workflow projection contract, `GET /api/repositories/{repositoryId}/workflow`, across backend endpoint metadata, Rust Tauri transport, and the TypeScript API wrapper.

## Capability

Canonical contract Oracle.

## Files Changed

- `tests/CommandCenter.Backend.Tests/ContractRequestBoundaryTests.cs`
- `docs/contracts.md`
- `docs/architectural-mechanisms.md`
- `docs/architectural-capabilities.md`

## Evidence

- Added backend route verification for `GET /api/repositories/{repositoryId:guid}/workflow`.
- Added Rust command verification for `get_workflow_projection(repository_id: String) -> Result<Value, String>`.
- Added TypeScript API wrapper verification for `getWorkflowProjection(repositoryId: string)`.
- Verified the Rust workflow command uses `backend_get_value` and does not construct a request body.
- Verified the TypeScript wrapper invokes `get_workflow_projection` with only `{ repositoryId }`.

## Request Boundary Findings

- The backend workflow projection endpoint is a bodyless GET with one required GUID route parameter.
- The Rust shell remains passive for this primary workflow response path by forwarding through `serde_json::Value`.
- The TypeScript API wrapper preserves the single repository-id command argument and does not add request fields.

## Verification

- `dotnet test tests/CommandCenter.Backend.Tests/CommandCenter.Backend.Tests.csproj --filter FullyQualifiedName~ContractRequestBoundaryTests`
  - Result: 9 tests passed.

## Limits

- No workflow artifact freshness manifest was added.
- No populated `decisionSession` fixture variant was added.
- No sibling workflow endpoint fixture or request-boundary verifier was added.
- No dev mock workflow handler was implemented or verified.
- No local workflow Oracle certification exists yet.
- Existing untracked `docs/audits/` content was left untouched.

## Rollback

Remove the three workflow request-boundary tests from `ContractRequestBoundaryTests` and revert the documentation status updates. The workflow fixture comparison and TypeScript consumer verification slices remain valid, but workflow request-shape drift would no longer be executable.
