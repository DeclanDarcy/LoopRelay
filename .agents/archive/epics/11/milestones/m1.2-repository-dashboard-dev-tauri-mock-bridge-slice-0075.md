# M1.2 Repository Dashboard Dev Tauri Mock Bridge Slice 0075

## Objective

Bridge the repository-dashboard `devTauriMock` dashboard entry to the generated `RepositoryDashboardConsumerCandidateProjection` without changing mock runtime behavior.

## Scope

- Changed `src/CommandCenter.UI/src/devTauriMock.ts` so `dashboardEntry(workspace)` returns `RepositoryDashboardConsumerCandidateProjection` imported from the generated repository-dashboard artifact.
- Preserved existing manual mock payload construction from `Workspace` state.
- Added backend consumer-verification coverage that prevents the dashboard mock bridge from silently reverting to the manual compatibility-wrapper alias.

## Affected Files

- `src/CommandCenter.UI/src/devTauriMock.ts`
- `tests/CommandCenter.Backend.Tests/ContractConsumerVerificationTests.cs`
- `.agents/milestones/m1.2-generated-contracts.md`
- `docs/contracts.md`
- `docs/architectural-capabilities.md`
- `docs/architectural-mechanisms.md`

## Authority and State Impact

This slice does not move semantic authority. The mock remains a development/test consumer and still derives its dashboard payload from existing mock workspace state. The new bridge makes its repository-dashboard return shape depend on the generated production-consumer candidate already governed by M1.2 metadata.

No mutable state ownership changed.

## Compatibility Impact

Existing frontend mock command behavior is unchanged. `list_repositories` still returns cloned `dashboardEntry` payloads from the same mock state. This is not generated mock replacement, direct product generated import authorization, or compatibility-wrapper retirement.

## Verification

- `dotnet test tests/CommandCenter.Backend.Tests/CommandCenter.Backend.Tests.csproj --filter FullyQualifiedName~ContractConsumerVerificationTests`: passed, 15 tests.
- `npm run build` in `src/CommandCenter.UI`: passed, with existing Vite chunk-size warning.

## Known Limits

- The mock payload is still hand-constructed and only bridged through the generated candidate return type.
- The bridge covers repository dashboard `dashboardEntry`; repository workspace and other mock commands remain pending.
- Generated mock artifact output and stale generated mock freshness checks remain pending.
- Direct generated imports by production UI remain unauthorized outside the existing compatibility-wrapper path.

## Rollback

Restore the prior `DashboardEntry` alias import from `./types`, remove the generated candidate import and bridge regression, then rerun the focused consumer-verification tests and UI build.
