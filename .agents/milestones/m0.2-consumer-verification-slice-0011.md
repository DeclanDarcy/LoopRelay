# Milestone 0.2 Consumer Verification Slice 0011

Date: 2026-06-26

## Objective

Add the first downstream consumer verification mechanism for the repository dashboard Contract Oracle pilot without redefining backend serialization authority.

## Scope

- Added `ContractConsumerVerificationTests.RepositoryDashboardRustMirrorReportsKnownDecisionSessionSummaryOmission`.
- The verifier reads `repository-dashboard.golden.json` as backend Oracle-observed truth.
- The verifier parses the Rust shell `RepositoryDashboardProjection` mirror in `src/CommandCenter.Shell/src/main.rs`.
- The verifier compares top-level JSON field names and reports downstream drift.

## Result

The test records the known consumer drift:

- Missing downstream field: `$[].decisionSessionSummary`.
- Consumer: Rust shell `RepositoryDashboardProjection`.

This is consumer drift evidence only. It does not weaken the backend Oracle fixture and does not authorize the Rust mirror as contract truth.

## Limits

- Coverage is top-level fields only.
- The verifier only covers the Rust repository dashboard mirror.
- Nested shape comparison, TypeScript type verification, dev mock verification, generated-artifact freshness, and semantic reinterpretation classification remain pending.
- The known Rust mirror drift is intentionally not corrected in this slice.

## Verification

- `dotnet test tests/CommandCenter.Backend.Tests/CommandCenter.Backend.Tests.csproj --filter FullyQualifiedName~ContractConsumerVerificationTests`

## Next Work

Generalize the consumer verification helper enough to compare nested repository dashboard shape, then add TypeScript/manual mock visibility without making either downstream representation authoritative.
