# Milestone 0.2 Slice 0028: Workflow Instance Golden Fixture

Date: 2026-06-26

## Objective

Capture the first primary workflow projection golden fixture for `WorkflowInstance` and add backend serialization comparison only.

This slice intentionally stops before workflow consumer verification, artifact freshness, request-boundary verification, dev mock verification, populated `decisionSession` coverage, sibling workflow endpoint fixtures, or local workflow Oracle certification.

## Scope

Contract identity: Workflow projection.

Primary endpoint: `GET /api/repositories/{repositoryId}/workflow`.

Backend contract identity: `WorkflowInstance`.

Authority: backend-owned workflow projection shape after backend HTTP JSON serialization.

Out of scope:

- workflow TypeScript/Rust/dev mock consumer verification,
- workflow artifact freshness manifest,
- workflow request-boundary verifier,
- populated `decisionSession` variant,
- sibling workflow endpoint fixtures,
- local workflow Oracle certification.

## Changes

- Added `tests/CommandCenter.Backend.Tests/ContractFixtures/workflow-instance.golden.json`.
- Added `ContractOracleFixtureTests.WorkflowInstanceGoldenFixtureMatchesBackendSerialization`.
- Added a representative `WorkflowInstance` constructor in the Oracle fixture tests.
- Updated `docs/contracts.md`, `docs/architectural-mechanisms.md`, and `docs/architectural-capabilities.md` to record the new workflow fixture comparison status.

## Representative Field-Category Coverage Checklist

| Category | Fixture coverage |
| --- | --- |
| Lifecycle enum | `currentStage`, `progressState`, `blockingGate`, nested statuses, transition stages, gate statuses, and timeline event types are serialized as backend enum strings. |
| Compatibility boolean | Top-level `hasPendingGitChanges` and `hasUnpushedGitChanges` are represented alongside `currentGit.hasPendingChanges` and `currentGit.hasUnpushedChanges`. |
| Explicit null | `executionFailure`, nullable timestamps, nullable ids, transition `blockingCondition`, completion artifact, gate satisfaction fields, and `decisionSession` are explicit nulls. |
| Nested object | Execution, handoff, decision, operational context, Git, completion, gate diagnostics, projection diagnostics, state-machine diagnostics, transitions, gates, and gate evidence are present. |
| Array | `nextPossibleStages`, `validTransitions`, `blockedTransitions`, `timeline`, `openGates`, `satisfiedGates`, `gateHistory`, evidence arrays, and diagnostics arrays are present. |
| Timeline | `timeline` has ordered execution, decision, and operational-context entries with timestamps, source domains, artifacts, and fingerprints. |
| Transition | Valid and blocked transition arrays cover `gateResolution`, null `blockingCondition`, populated `blockingCondition`, and canonical transition metadata. |
| Gate | Open, satisfied, and gate-history arrays cover command fields, status, source evidence, satisfied timestamp nullability, and evidence fingerprints. |
| Diagnostic | Execution, handoff, decision, operational-context, Git, gate, projection, and state-machine diagnostics include required arrays. |
| Eligibility | Backend-owned booleans for execution, decision, governance, operational-context review/promotion/commit, Git commit/push requirements, and gate openness are represented. |
| `decisionSession = null` | The first fixture explicitly serializes `decisionSession: null`. A populated variant remains pending. |

## Verification

Command:

```powershell
dotnet test tests/CommandCenter.Backend.Tests/CommandCenter.Backend.Tests.csproj --filter FullyQualifiedName~ContractOracleFixtureTests
```

Result:

- Passed: 6
- Failed: 0
- Skipped: 0

## Current Limits

- Workflow fixture comparison exists only for the primary `WorkflowInstance` shape.
- No workflow consumer verification exists for `src/CommandCenter.UI/src/types/workflow.ts`, Rust shell mirrors, or dev mock payloads.
- No workflow artifact freshness manifest exists.
- No workflow request-boundary verifier exists for `GET /api/repositories/{repositoryId}/workflow`.
- No populated `decisionSession` fixture variant exists.
- Sibling workflow endpoints remain excluded.
- No local workflow Oracle certification exists.
- Milestone 0.2 remains active and uncertified globally.

## Next Gate

Add workflow consumer verification for the manual TypeScript workflow contract shape against `workflow-instance.golden.json`, while keeping Rust shell drift and dev mock absence as reported downstream consumer findings rather than contract authority.
