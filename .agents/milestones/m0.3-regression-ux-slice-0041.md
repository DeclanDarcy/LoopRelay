# Milestone 0.3 Regression UX Slice 0041

Objective: define and guard the architectural regression failure-message contract.

Owning capability: Architectural regression framework.

New state:

- Added `### Regression UX Specification` to `docs/architectural-mechanisms.md`.
- Architectural regression failures now require invariant, architectural intent, observed drift, owner, severity, detection confidence, evidence expectation, remediation path, and escalation guidance.
- Detection confidence is explicitly scoped to the detection mechanism, not to the architectural decision.
- Extended `tests/CommandCenter.Backend.Tests/Architecture/ArchitecturalRegressionFrameworkTests.cs` with `RegressionUxSpecificationDefinesStructuredFailureMessages`.
- Updated `.agents/milestones/m0.3-regression-framework.md` to mark the failure-message requirement and regression UX output complete.
- Updated `docs/architectural-capabilities.md` to record the regression UX guard as active M0.3 protection.

Authority and state impact:

- No production authority or mutable state changed.
- The slice governs how future regressions communicate architectural failures.

Compatibility impact:

- No runtime or contract compatibility impact.

Rollback path:

- Revert the documentation and meta-regression additions from this slice.
- If a later verifier cannot provide all required fields, quarantine that verifier with owner, reason, affected invariant, and retirement criteria rather than weakening the UX specification silently.

Verification:

- `dotnet test tests/CommandCenter.Backend.Tests/CommandCenter.Backend.Tests.csproj --filter ArchitecturalRegressionFrameworkTests` passed: 9 passed, 0 failed, 0 skipped.
- `git diff --check` passed with line-ending normalization warnings only.

Known limits:

- This slice defines the failure-message contract and guards the durable specification. It does not retrofit every existing assertion message across all architecture tests.
- Detection confidence is introduced as failure-message metadata here; the broader architectural confidence model remains pending in M0.3.
