# Milestone 0.2 Slice 0031: Workflow Artifact Freshness

Date: 2026-06-26

## Objective

Add artifact freshness verification for the primary workflow projection contract, tying the accepted workflow Oracle fixture to the manual TypeScript workflow contract artifact.

## Capability

Canonical contract Oracle.

## Files Changed

- `tests/CommandCenter.Backend.Tests/ContractFixtures/workflow-instance.artifact-freshness.json`
- `tests/CommandCenter.Backend.Tests/ContractGeneratedArtifactFreshnessTests.cs`
- `docs/contracts.md`
- `docs/architectural-mechanisms.md`
- `docs/architectural-capabilities.md`
- `docs/contract-endpoint-catalog.md`

## Evidence

- Added `workflow-instance.artifact-freshness.json`.
- The manifest hashes `tests/CommandCenter.Backend.Tests/ContractFixtures/workflow-instance.golden.json` as the Oracle source.
- The manifest hashes `src/CommandCenter.UI/src/types/workflow.ts` as the Phase 0 verified manual TypeScript contract artifact.
- Added `ContractGeneratedArtifactFreshnessTests.WorkflowInstanceTypeScriptContractArtifactMatchesFreshnessManifest`.

## Mechanism Reuse

This slice reused the existing manifest model, verifier, artifact kind, and failure taxonomy without changing `ContractGeneratedArtifactFreshnessVerifier`.

Existing verifier regression tests continue to cover:

- stale source with unchanged artifact,
- unexpected artifact modification,
- missing expected artifact.

## Verification

- `dotnet test tests/CommandCenter.Backend.Tests/CommandCenter.Backend.Tests.csproj --filter FullyQualifiedName~ContractGeneratedArtifactFreshnessTests`
  - Result: passed, 6 tests.

## Limits

- The workflow TypeScript artifact is still a manual Phase 0 verified contract artifact, not generated Milestone 1.2 output.
- No populated `decisionSession` workflow fixture variant was added.
- No dev mock workflow handler was implemented or verified.
- No sibling workflow endpoint fixture or freshness manifest was added.
- No local workflow Oracle certification exists yet.
- Existing untracked `docs/audits/` content existed before this slice and was left untouched.

## Rollback

Remove `workflow-instance.artifact-freshness.json`, remove `WorkflowInstanceTypeScriptContractArtifactMatchesFreshnessManifest`, and revert the documentation status updates. The workflow fixture comparison, TypeScript consumer verification, and request-boundary verification slices remain valid, but workflow fixture/type artifact freshness would no longer be executable.
