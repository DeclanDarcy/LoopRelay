# Milestone 1.2: Build the Generated Contract Ecosystem

Objective: replace parallel hand-maintained contract truths with deterministic generated or mechanically verified artifacts.

Current slice state:

- Slice 0068 starts M1.2 as generation-pipeline validation, not consumer migration.
- The first pilot family is `repository-dashboard`, reusing the accepted Phase 0 Oracle fixture.
- The executable path is Oracle fixture -> canonical generation IR -> generated TypeScript contract metadata and raw aliases -> freshness manifest.
- Slice 0069 extends the generated artifact with raw TypeScript aliases and verifies `RepositoryDashboardGeneratedProjection` against the Oracle fixture.
- Slice 0070 defines the generated TypeScript consumer policy for schema/nullability and semantic alias boundaries before production consumer migration.
- Slice 0071 adds the first governed schema metadata pilot for `repository-dashboard`, carrying explicit presence, nullability, semantic domains, enum values, identity roles, array ordering, and string formats into the generated IR and generated TypeScript metadata.
- Slice 0073 emits the first repository-dashboard generated production-consumer candidate from governed metadata and verifies it structurally and semantically against the manual compatibility wrapper.
- Slice 0074 bridges the production `RepositoryDashboardProjection` compatibility wrapper to the generated production-consumer candidate while preserving the existing `src/CommandCenter.UI/src/types/repositories.ts` import boundary.
- No direct production UI import from the generated artifact, Rust shell mirror, dev Tauri mock, or API wrapper has been migrated by these slices.
- The generated raw aliases are fixture-observed rather than schema-complete; the generated production-consumer candidate is now consumed through the compatibility wrapper only.

Implementation tasks:

- [ ] Define generated artifact categories: TypeScript types, Rust shell contract shapes or opaque command metadata, mocks, fixtures, docs, OpenAPI/JSON schema if useful, test contracts, and future SDKs.
- [ ] Add a contract intermediate representation if direct generation from backend serialization metadata is too brittle.
- [x] Implement deterministic generation for the first raw TypeScript contract aliases under `src/CommandCenter.UI/src/contracts/generated`.
- [x] Pilot governed schema metadata in the repository-dashboard IR and generated TypeScript metadata artifact without migrating production consumers.
- [x] Emit and verify the first generated repository-dashboard production-consumer candidate without migrating production consumers.
- [ ] Replace or wrap hand-written types in `src/CommandCenter.UI/src/types/*` with generated exports, leaving compatibility aliases only where needed. Repository dashboard is bridged through the existing compatibility wrapper as of Slice 0074; the broader `src/types` surface remains pending.
- [ ] Generate or mechanically verify Tauri command metadata and route maps instead of maintaining domain response mirrors in Rust.
- [ ] Generate or contract-verify `src/CommandCenter.UI/src/devTauriMock.ts` fixtures.
- [ ] Add stale generated artifact detection to local verification and CI.
- [ ] Add guardrails that generated files are not manually edited.
- [ ] Migrate consumers incrementally, starting with low-risk read models, then mutation results, then complex workflow/decision/execution models.

Required outputs:

- [ ] Generated artifact catalog.
- [ ] Generation architecture and intermediate representation.
- [ ] Generator taxonomy.
- [x] Consumer lifecycle model.
- [ ] Determinism specification.
- [ ] Generation verification model.
- [x] Ownership model.
- [ ] Manual artifact retirement plan.
- [ ] Generation toolchain.
- [ ] Generator evolution model.
- [x] Canonical generated examples for repository dashboard metadata and raw aliases.
- [x] Generated TypeScript consumer policy for raw aliases, production consumer types, compatibility wrappers, nullability, omission, semantic aliases, identities, array ordering, and arbitrary text.
- [x] Governed repository-dashboard schema metadata pilot for selected production-migration blockers.
- [x] Governed repository-dashboard production-consumer candidate with structural and semantic equivalence checks against the compatibility wrapper.
- [x] Repository-dashboard compatibility-wrapper bridge that keeps existing production imports stable while sourcing dashboard and decision-session summary types from the generated production-consumer candidate.

Exit criteria:

- [ ] At least one complete backend projection flows through oracle fixture, generated TypeScript, generated/verified mock, and consumer test.
- [ ] Generation is deterministic and stale artifacts fail verification.
- [ ] Manual consumer artifacts have an explicit migration or retirement path.
