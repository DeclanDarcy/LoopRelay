# Milestone 0.1 Follow-On: Shell Error-Envelope Passivity Preparation Slice 0005

Date: 2026-06-26.

## Objective

Add the next small executable transport-passivity protection before beginning Milestone 0.2 contract Oracle work.

## Capability

Structural verification and passive transport preparation.

## Changes

- Added Rust shell regression `backend_get_value_preserves_boundary_violation_error_envelope`.
- Generalized the local shell test server helper from successful JSON only to arbitrary HTTP status responses.
- Updated `docs/architectural-mechanisms.md` to record boundary-violation error-envelope relay as protected for the generic GET value helper.
- Updated `docs/architectural-capabilities.md` to reflect the current shell passive relay coverage.

## Architectural Impact

- Authority remains backend-owned.
- The shell still does not interpret the boundary violation payload semantically in the protected generic GET helper path.
- The regression preserves nested backend-owned error details, explicit null, and empty arrays inside `boundaryViolation`.

## Compatibility Impact

No production command behavior changed intentionally. The slice adds regression coverage around the existing `response_error` behavior for backend boundary-violation envelopes.

## Remaining Gaps

- Non-boundary backend error responses are still collapsed to the backend `error` string.
- POST relay behavior is not yet covered by this mechanism family.
- Command-family classification and domain-shaped Rust mirror retirement remain open.

## Rollback

Remove the new Rust test/helper generalization and restore the prior successful-response-only helper. No production migration would need rollback.

## Verification

Completed:

- `cargo fmt` in `src/CommandCenter.Shell`.
- `cargo test` in `src/CommandCenter.Shell`: 2 passed.
- `cargo build` in `src/CommandCenter.Shell`.
