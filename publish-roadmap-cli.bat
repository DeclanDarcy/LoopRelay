@echo off
setlocal

echo LoopRelay.Roadmap.CLI is no longer published as a public executable.
echo Use publish-cli.bat to publish the unified LoopRelay.CLI, then run one of:
echo   looprelay traditional
echo   looprelay eval

exit /b 1
