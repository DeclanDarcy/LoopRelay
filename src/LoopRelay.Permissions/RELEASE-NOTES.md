# LoopRelay.Permissions Release Notes

## Unreleased

- **Fix:** operator-configured permission rules now take effect in rule evaluation; previously only the hard-deny floor and invariant guard were honored.
