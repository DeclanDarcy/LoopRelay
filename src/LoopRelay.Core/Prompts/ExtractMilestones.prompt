Read .agents/plan.md and split it into one markdown file per milestone: .agents/milestones/m*.md where * is the number plus a succinct label
Each milestone markdown should be formatted such that progress can be tracked using checkboxes for itemized work
Modify .agents/plan.md such that its milestone entries no longer contain any content other than a (See ./relative/path) pointer to the new markdown file using relative path from .agents/plan.md
